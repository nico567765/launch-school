The first program would run fine and output '3' to screen. The second program would produce a name error because x has been initialised as a variable only within the block passed to #times and so no variable called 'x' exists outside that scope.